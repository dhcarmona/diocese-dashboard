package org.iecr.diocesedashboard.webapp.controller;

/** Discriminator for unified template item references (info items vs section headers). */
public enum TemplateItemKind {
  INFO_ITEM,
  SECTION_HEADER
}
